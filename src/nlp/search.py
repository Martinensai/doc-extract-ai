"""search.py
Semantic search wrapper that queries the vector index and returns candidates.
"""
def semantic_search(query, top_k=5):
    # TODO: call the vector DB and return results with scores
    return []
