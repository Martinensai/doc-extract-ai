"""retriever.py
High-level retriever that would perform RAG: query vector DB, build context, call LLM.
"""
def retrieve_answer(query: str) -> str:
    # TODO: implement retrieval from vector DB and call LLM
    # Placeholder:
    return "Réponse factice — configure le retriever pour retourner le vrai contenu."
